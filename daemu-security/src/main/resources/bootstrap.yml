server:
  port: 9999
  servlet:
    context-path: /authcenter
spring:
  application:
    name: daemu-security
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
#        prefix: daemu-security
#        file-extension: yml
        group: DAEMU_GROUP
      discovery:
        server-addr: localhost:8848
        group: DAEMU_GROUP
logging:
  level:
    com:
     alibaba:
      cloud:
        nacos:
          client: debug
